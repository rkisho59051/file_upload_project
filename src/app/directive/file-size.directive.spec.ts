import { FileSizeDirective } from './file-size.directive';

describe('FileSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSizeDirective();
    expect(directive).toBeTruthy();
  });
});
